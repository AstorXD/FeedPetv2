{
  "rules": {
    ".read": true,
    ".write": true,
    "feeding_history": {
      ".indexOn": ["timestamp"]
    },
    "settings": {
      "schedule": {
        ".indexOn": ["updatedAt"]
      }
    }
  }
}